import org.junit.Test;
import org.junit.jupiter.api.Test;

public class TestEmploye {

    @Test
    public void testAddFurstTaskToUser();
    user testName = new Name("Test User1");
    Task testOrder = nwe Oreder(name: "Test name", list: "Order");
    Main.addTask(testName, testOreder);
    int expected TasksCount = 1;
    int actual TasksCount = testUser.getTasks().size();
    assertEquals(expectedTasksCount, actualTasksCount);
}
